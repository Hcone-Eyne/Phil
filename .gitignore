# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/

# ── PyInstaller / packaging (do not commit build artefacts) ───────────────────
build/
dist/

# ── Secrets & per-machine config (never commit) ─────────────────────────────
.env
.env.*
phil_config.json
voice_input/.env
voice_input/Keys/phil_config.json

# voice_input/Keys/config.py is SOURCE — tracked on purpose (do not add Keys/ here)

# ── Generated CAD output & runtime logs ───────────────────────────────────────
output/
logs/
*.step
*.stp
*.obj

# Runtime JSON (logs/ already covers most; explicit for files outside logs/)
voice_input/data/
voice_input/cad_assist/generated_asset_library.json

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
